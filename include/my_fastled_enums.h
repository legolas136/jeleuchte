#ifndef MY_FASTLED_ENUMS
#define MY_FASTLED_ENUMS

enum AnimationDirection
{
  FORWARD,
  BACKWARD,
  FROM_TO
};

enum AnimationMirrorDirection
{
  INNER_TO_OUTER,
  OUTER_TO_INNER,
  OUTER_TO_INNER_DOUBLE
};

#endif